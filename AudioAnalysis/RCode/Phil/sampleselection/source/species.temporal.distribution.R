

GetSpeciesMinuteDf <- function (sites, dates) {
    # creates a dataframe of species (cols) and minutes of the day (rows)
    # the value in each cell is an integer representing how many of the
    # site/date combinations had that species at that minute
    
    
    
    
}
