this file only contains block.
so just applied the block clustering,


parameters:

timeBetw=0.05;
frameThreshold=30;
freqNThe=2;
freqDistance=1;
freNumbThe=2;
frmInFrq=3;


[h1,S1]=ChebyshevFilter(10,0.1,5500,8000,fs/2,y);

[h2,S2]=ChebyshevFilter(10,0.1,6000,8500,fs/2,y);

[h3,S3]=ChebyshevFilter(10,0.1,7000,9000,fs/2,y);